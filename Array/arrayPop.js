// El metodo pop() elimina el ultimo elemento de un array y devuelve ese elemento. Este medoto cambia la longitud del array

const cities = ['Madrid', 'Valencia', 'Lugo'];
const last = cities.pop();
console.log(cities); // [ 'Madrid', 'Valencia' ]
console.log(last); // Lugo
